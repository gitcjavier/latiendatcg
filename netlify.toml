[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.11.0"

# El adapter @astrojs/netlify genera automáticamente las funciones
# en .netlify/ — no hace falta config extra de redirects.
